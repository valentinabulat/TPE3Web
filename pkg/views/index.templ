package views

import "github.com/valentinabulat/TPE3Web/internal/db" 

templ IndexPage(products []db.ListProductosRow) {
    @Layout("Lista de Productos") {
        <h1>Productos</h1>
        @ProductForm()
        @ProductList(products)
    }
}
